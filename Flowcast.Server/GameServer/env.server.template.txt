# Copy to VPS as .env (e.g. /srv/bstd/.env). Replace CHANGE_ME_STRONG_PASSWORD.
#
# Why separate Dockerfile and docker-compose?
# - Dockerfile: builds ONE image (the .NET app). Used by CI to build & push.
# - docker-compose: runs MULTIPLE containers (db + flowcast). flowcast needs db.
# - db and flowcast are separate so: db persists data; app can restart independently; scaling.

# --- .env content ---

ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:8080

FLOWCAST_IMAGE=registry.kingkodegames.ir/flowcast:latest

POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

Identity__ConnectionString=Host=db;Port=5432;Database=Flowcast;Username=postgres;Password=CHANGE_ME_STRONG_PASSWORD
PlayerProgress__ConnectionString=Host=db;Port=5432;Database=Flowcast;Username=postgres;Password=CHANGE_ME_STRONG_PASSWORD
Shop__ConnectionString=Host=db;Port=5432;Database=Flowcast;Username=postgres;Password=CHANGE_ME_STRONG_PASSWORD

Identity__TokenOptions__Issuer=https://api.bstd.kingkodegames.ir
Identity__TokenOptions__Audience=flowcast-game
Identity__TokenOptions__AccessTokenExpiryMinutes=15
Identity__TokenOptions__RefreshTokenExpiryDays=30

Database__Provider=Postgres

Serilog__WriteTo__1__Args__ServerUrl=http://seq:5341

Cors__AllowedOrigins__0=http://localhost:4200
Cors__AllowedOrigins__1=https://cms.kingkodegames.ir

# --- Add this "db" service to docker-compose.yml (flowcast depends on it) ---
#
#  db:
#    image: postgres:17-alpine
#    container_name: flowcast-postgres
#    environment:
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#      POSTGRES_DB: Flowcast
#    volumes:
#      - pg_db:/var/lib/postgresql/data
#    networks:
#      - edge-infra
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U postgres"]
#      interval: 5s
#      timeout: 5s
#      retries: 5
#
# Add to flowcast service:  depends_on: { db: { condition: service_healthy } }
# Add to flowcast networks so it can reach db (same network as db)
